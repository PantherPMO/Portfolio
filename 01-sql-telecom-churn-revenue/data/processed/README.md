# Processed Data

CSV intermediates produced by `sql/00_setup/convert_xlsx_to_csv.py`, and any cleaned outputs.

**Not committed** — regenerable from `data/raw/` in one command, and derived from source files whose licence is unresolved (D-09).
